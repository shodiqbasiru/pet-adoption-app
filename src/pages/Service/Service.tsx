import React from 'react';

const Service = ():React.ReactElement => {
    return (
        <div className="min-h-screen">
            <h1 className="mt-24">Service Page</h1>
        </div>
    );
};

export default Service;